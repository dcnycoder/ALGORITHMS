const { start } = require("repl")

mergeSort(arr, start=0, end=arr.length-1) {
  if (start>=end) return arr
  else {

  }
  return arr
  function merge(arr, start, middle, end) {
    
  }
}


const arr = [1,4,0,200,5,70]
console.log(mergeSort(arr))